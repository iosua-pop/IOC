{
  "name": "BrightBuds",
  "version": "1.0.0",
  "description": "Aplicația mea desktop",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:win": "electron-packager . BrightBuds --platform=win32 --arch=x64 --out=dist --overwrite",
    "build:mac": "electron-packager . BrightBuds --platform=darwin --arch=x64 --out=dist --overwrite"

  },
  "author": "BrightBuds",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  }
}
